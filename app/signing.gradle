android {

    signingConfigs {
        releaseConfig {
            storeFile file("keystore.jks")
            storePassword 'android'
            keyAlias "androiddebugkey"
            keyPassword 'android'
        }
    }
}